## Engineer-In-The-Loop Git Rule

Do not commit, push, tag, stash, reset, create worktrees, or otherwise manage git history unless the engineer explicitly asks.

Use git for read-only inspection such as `git status`, `git diff`, and `git log` when useful.
