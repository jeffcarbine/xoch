---
name: xoch-help
description: List every Xoch command with its description, generated from each prompt file's own frontmatter
---

# Xoch - Help

This is read-only and does not touch job or workflow state.

Run the helper and print its output verbatim -- no summarization, no reformatting:

```bash
~/.xoch/bin/help-actions.js list
```
